<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:views="clr-namespace:EasyTabata.Views"
             mc:Ignorable="d"
             x:Class="EasyTabata.Views.PlayingTabataView"
             x:Name="PlayingView">
  <ContentPage.Content>
        <StackLayout Margin="10">
            <ProgressBar Progress="{Binding CurrentProgress}"/>
            <Label Text="{Binding RemainingTime}"/>
            <Label Text="{Binding CurrentState}"/>
            <Label Text="{Binding CurrentRound}"/>
            <Label Text="{Binding CurrentExercise}"/>
            <FlexLayout Direction="Row" AlignContent="SpaceBetween">
                <Button Text="Play" Clicked="Play_Clicked" />
                <Button Text="Stop" Clicked="Stop_Clicked" />
                <Button Text="Reset" Clicked="Reset_Clicked" />
            </FlexLayout>
        </StackLayout>
  </ContentPage.Content>
</ContentPage>