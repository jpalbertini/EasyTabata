<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="EasyTabata.Views.SettingsView">
    <StackLayout Margin="4,0,4,0">
        <Grid VerticalOptions="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Label VerticalTextAlignment="Center" Text="Enable Sound:" />
            <Switch IsToggled="{Binding EnableSound, Mode=TwoWay}" ThumbColor="{StaticResource NavigationPrimary}"/>
        </Grid>
    </StackLayout>
</ContentPage>